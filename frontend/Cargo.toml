[package]
name = "frontend"
version = "0.1.0"
edition = "2021"


[dependencies]
tui = "0.16"
termion = "1.5"
tokio = { version = "1", features = ["full"] }
tokio-tungstenite = "0.15"  # For WebSocket client
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
